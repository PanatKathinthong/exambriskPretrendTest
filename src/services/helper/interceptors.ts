import axios from 'axios';

// axios.interceptors.request.use(function (config) {
//   return new Promise((resolve) => {
//     if (process.env.NODE_ENV === 'test') return resolve(config);
//     resolve(config);
//   });
// });

export default axios;
